syntax = "proto3";

package flom.proto;

import "frame.proto";

message Motion {
  string model_id = 1;
  enum Loop {
    None = 0;
    Wrap = 1;
  }
  Loop loop = 2;
  map<string, double> initial_positions = 3;
  repeated Frame frames = 4;
}
