syntax = "proto3";

import "frame.proto";

message Motion {
  string model_id = 1;
  enum Loop {
    None = 0;
    Wrap = 1;
  }
  Loop loop = 2;
  bool is_trained = 3;
  repeated Frame frames = 4;
}
